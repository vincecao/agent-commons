# OpenCode Profile

This file is managed by `agent-commons`.

Personal skills live in `~/.config/opencode/skills/` after sync. Keep private skill content in this repo's ignored `skills/` directory, then run `pnpm sync`.
